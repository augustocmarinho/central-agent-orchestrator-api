# 🏗️ Arquitetura do Sistema de Tokens

## Fluxo de Autenticação

```
┌─────────────┐
│    N8N      │
│  Workflow   │
└──────┬──────┘
       │ HTTP Request
       │ Header: X-System-API-Key: sat_abc123...
       ▼
┌──────────────────────────────────────────────────┐
│           Express Middleware Layer               │
├──────────────────────────────────────────────────┤
│                                                  │
│  1. systemAuthMiddleware                         │
│     ├─ Extrai token do header                   │
│     ├─ Valida token no banco                    │
│     ├─ Verifica expiração                       │
│     ├─ Valida IP do cliente                     │
│     └─ Registra uso (log)                       │
│                                                  │
│  2. Fallback: SYSTEM_API_KEYS (legado)          │
│     └─ Se token não existe no banco             │
│                                                  │
└──────────────────┬───────────────────────────────┘
                   │ ✅ Autenticado
                   ▼
        ┌─────────────────────┐
        │  Route Handler      │
        │  (Controller)       │
        └─────────────────────┘
```

## Estrutura do Banco de Dados

```
┌──────────────────────────────┐
│     system_tokens            │
├──────────────────────────────┤
│ id              UUID PK      │
│ name            VARCHAR      │  ← "N8N Production"
│ token           VARCHAR      │  ← "sat_abc123..."
│ description     TEXT         │
│ allowed_ips     TEXT[]       │  ← ["192.168.1.100", "10.0.0.0/8"]
│ is_active       BOOLEAN      │  ← true/false
│ expires_at      TIMESTAMP    │  ← NULL = nunca expira
│ last_used_at    TIMESTAMP    │
│ last_used_ip    VARCHAR      │
│ created_by      UUID FK      │  → users.id
│ created_at      TIMESTAMP    │
│ updated_at      TIMESTAMP    │
└──────────────────────────────┘
         │ 1
         │
         │ N
         ▼
┌──────────────────────────────┐
│  system_token_logs           │
├──────────────────────────────┤
│ id                UUID PK    │
│ system_token_id   UUID FK    │
│ ip_address        VARCHAR    │
│ path              VARCHAR    │  ← "/api/agents/123"
│ method            VARCHAR    │  ← "GET"
│ status_code       INTEGER    │  ← 200
│ success           BOOLEAN    │
│ error_message     TEXT       │
│ created_at        TIMESTAMP  │
└──────────────────────────────┘
```

## Camadas da Aplicação

```
┌─────────────────────────────────────────────────────────────┐
│                      HTTP Layer                             │
│                 (Express Routes)                            │
├─────────────────────────────────────────────────────────────┤
│  /api/system-tokens                                         │
│  ├─ POST   /          → systemTokenController.create       │
│  ├─ GET    /          → systemTokenController.list         │
│  ├─ GET    /:id       → systemTokenController.getById      │
│  ├─ DELETE /:id       → systemTokenController.revoke       │
│  ├─ PUT    /:id/allowed-ips → updateAllowedIps             │
│  └─ GET    /:id/logs  → systemTokenController.getLogs      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Controller Layer                          │
│           (systemToken.controller.ts)                       │
├─────────────────────────────────────────────────────────────┤
│  • Validação de entrada (Zod)                              │
│  • Verificação de permissões (admin only)                  │
│  • Logging de ações                                        │
│  • Formatação de respostas                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Service Layer                            │
│           (systemToken.service.ts)                          │
├─────────────────────────────────────────────────────────────┤
│  • generateSecureToken()    → Gera token único             │
│  • createToken()            → Cria no banco                │
│  • validateToken()          → Valida + verifica IP         │
│  • checkIpAllowed()         → IP/CIDR/Wildcard             │
│  • updateLastUsed()         → Atualiza uso                 │
│  • logTokenUsage()          → Registra log                 │
│  • revokeToken()            → Desativa token               │
│  • updateAllowedIps()       → Atualiza IPs                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Database Layer                             │
│                 (PostgreSQL)                                │
├─────────────────────────────────────────────────────────────┤
│  • system_tokens          (dados dos tokens)               │
│  • system_token_logs      (auditoria)                      │
│  • users                  (criadores dos tokens)           │
└─────────────────────────────────────────────────────────────┘
```

## Validação de IP

```
┌─────────────────────────────────────────────────────┐
│          checkIpAllowed(clientIp, allowedIps)       │
└─────────────────────────────────────────────────────┘
                       │
                       ▼
         ┌─────────────────────────┐
         │  Normalizar IP          │
         │  (remover ::ffff:)      │
         └────────┬────────────────┘
                  │
        ┌─────────▼─────────────┐
        │  Para cada allowed_ip │
        └─────────┬─────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
    ▼             ▼             ▼
┌─────────┐  ┌─────────┐  ┌──────────┐
│ Exato?  │  │ CIDR?   │  │Wildcard? │
│192.168  │  │10.0.0.0 │  │192.168.* │
│.1.100   │  │   /8    │  │          │
└────┬────┘  └────┬────┘  └────┬─────┘
     │            │            │
     └────────────┴────────────┘
                  │
                  ▼
            ┌──────────┐
            │ Permitir │
            └──────────┘
```

### Exemplos de Validação:

| Client IP | Allowed IPs | Resultado |
|-----------|-------------|-----------|
| 192.168.1.100 | `["192.168.1.100"]` | ✅ Exato |
| 192.168.1.101 | `["192.168.1.0/24"]` | ✅ CIDR |
| 10.5.3.2 | `["10.0.0.0/8"]` | ✅ CIDR |
| 192.168.1.50 | `["192.168.1.*"]` | ✅ Wildcard |
| 8.8.8.8 | `["192.168.1.100"]` | ❌ Bloqueado |

## Fluxo de Criação de Token

```
┌──────────────┐
│ Admin User   │
└──────┬───────┘
       │ POST /api/system-tokens
       │ { name, description, allowed_ips, expires_at }
       ▼
┌──────────────────────────────────────┐
│  systemTokenController.create        │
│  1. Validar entrada (Zod)            │
│  2. Verificar role = admin           │
└──────────┬───────────────────────────┘
           ▼
┌──────────────────────────────────────┐
│  systemTokenService.createToken      │
│  1. Gerar token seguro               │
│     sat_ + 64 chars hex              │
│  2. Inserir no banco                 │
│  3. Retornar token + metadata        │
└──────────┬───────────────────────────┘
           ▼
┌──────────────────────────────────────┐
│  Resposta                            │
│  {                                   │
│    token: "sat_abc123...",           │
│    warning: "Não será exibido..."    │
│  }                                   │
└──────────────────────────────────────┘
```

## Fluxo de Validação (Requisição do N8N)

```
┌─────────────┐
│    N8N      │
└──────┬──────┘
       │ GET /api/agents/123
       │ Header: X-System-API-Key: sat_abc123...
       ▼
┌────────────────────────────────────────────┐
│  systemAuthMiddleware                      │
│  1. Extrair token do header                │
│  2. Obter IP do cliente                    │
│     - X-Forwarded-For (proxy)              │
│     - req.ip                               │
│     - socket.remoteAddress                 │
└──────┬─────────────────────────────────────┘
       ▼
┌────────────────────────────────────────────┐
│  systemTokenService.validateToken          │
│  1. Buscar token no banco                  │
└──────┬─────────────────────────────────────┘
       │
       ├─ Token não existe? → ❌ 401
       │
       ▼
┌────────────────────────────────────────────┐
│  2. Verificar is_active = true             │
└──────┬─────────────────────────────────────┘
       │
       ├─ Inativo? → ❌ 401
       │
       ▼
┌────────────────────────────────────────────┐
│  3. Verificar expiração                    │
└──────┬─────────────────────────────────────┘
       │
       ├─ Expirado? → ❌ 401
       │
       ▼
┌────────────────────────────────────────────┐
│  4. Verificar IP (se configurado)          │
│     checkIpAllowed(clientIp, allowed_ips)  │
└──────┬─────────────────────────────────────┘
       │
       ├─ IP não permitido? → ❌ 401
       │
       ▼
┌────────────────────────────────────────────┐
│  5. Atualizar last_used_at e last_used_ip  │
│  6. Registrar log de uso                   │
└──────┬─────────────────────────────────────┘
       │
       ▼
     ✅ Permitir acesso
       │
       ▼
┌────────────────────────────────────────────┐
│  Route Handler (agentController.getOne)    │
└────────────────────────────────────────────┘
```

## Auditoria e Logs

Cada requisição autenticada gera:

```javascript
{
  system_token_id: "uuid-do-token",
  ip_address: "192.168.1.100",
  path: "/api/agents/123",
  method: "GET",
  status_code: 200,
  success: true,
  error_message: null,
  created_at: "2024-01-20T15:45:00Z"
}
```

Estes logs podem ser consultados via:
```
GET /api/system-tokens/{id}/logs
```

## Compatibilidade com Sistema Legado

```
┌──────────────────────────────────────┐
│  systemAuthMiddleware                │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  1. Tentar validar no banco          │
│     validateToken()                  │
└──────────┬───────────────────────────┘
           │
           ├─ Token válido? → ✅ Permitir
           │
           ▼
┌──────────────────────────────────────┐
│  2. Fallback: SYSTEM_API_KEYS        │
│     config.systemApiKeys.includes()  │
└──────────┬───────────────────────────┘
           │
           ├─ Encontrado? → ✅ Permitir
           │
           ▼
         ❌ 401 Unauthorized
```

Isso garante que:
- ✅ Tokens antigos (env) continuam funcionando
- ✅ Novos tokens (banco) têm mais recursos
- ✅ Migração gradual sem downtime

## Segurança em Camadas

```
┌─────────────────────────────────────────────┐
│  Camada 1: Token Criptograficamente Seguro  │
│  • sat_ + crypto.randomBytes(32)           │
│  • 64 caracteres hexadecimais               │
│  • Impossível adivinhar                     │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Camada 2: Validação de IP                  │
│  • IP exato, CIDR ou Wildcard               │
│  • Previne uso de IP não autorizado         │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Camada 3: Expiração (opcional)             │
│  • Tokens podem ter data de validade        │
│  • Verificado a cada requisição             │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Camada 4: Ativação/Revogação               │
│  • is_active flag                           │
│  • Admin pode desativar instantaneamente    │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│  Camada 5: Auditoria Completa               │
│  • Todos os usos registrados                │
│  • IP, path, método, timestamp              │
│  • Detecta uso suspeito                     │
└─────────────────────────────────────────────┘
```

## Rotas e Permissões

| Rota | Método | Autenticação | Permissão |
|------|--------|--------------|-----------|
| `/api/system-tokens` | POST | JWT | Admin |
| `/api/system-tokens` | GET | JWT | Admin |
| `/api/system-tokens/:id` | GET | JWT | Admin |
| `/api/system-tokens/:id` | DELETE | JWT | Admin |
| `/api/system-tokens/:id/allowed-ips` | PUT | JWT | Admin |
| `/api/system-tokens/:id/logs` | GET | JWT | Admin |
| `/api/agents/*` | GET/POST | System Token OU JWT | - |
| `/api/plugins/*` | GET | System Token OU JWT | - |

## Scripts Disponíveis

```
npm run migrate
└─ Executa migrations/*.sql em ordem
   ├─ 001_initial_schema.sql
   └─ 002_system_tokens.sql ← Nova!

npm run setup:n8n-token
└─ Script interativo de criação
   ├─ Cria/usa admin
   ├─ Gera token seguro
   └─ Exibe instruções
```

## Arquivos Criados/Modificados

```
back/
├── src/
│   ├── db/
│   │   └── migrations/
│   │       └── 002_system_tokens.sql ............ Nova tabela
│   ├── services/
│   │   └── systemToken.service.ts ............... Lógica de negócio
│   ├── controllers/
│   │   └── systemToken.controller.ts ............ Endpoints
│   ├── middleware/
│   │   └── systemAuth.ts ....................... Atualizado (validação)
│   ├── routes/
│   │   └── index.ts ............................ Atualizado (rotas)
│   └── scripts/
│       └── setup-n8n-token.ts .................. Script de setup
├── SYSTEM_TOKENS.md ............................ Documentação completa
├── README_SYSTEM_TOKENS.md ..................... Guia rápido
├── ARCHITECTURE_SYSTEM_TOKENS.md ............... Este arquivo
├── .env.example ................................ Atualizado
└── package.json ................................ Novo script
```

## Performance e Escalabilidade

### Otimizações Implementadas:

1. **Índices no Banco:**
   ```sql
   CREATE INDEX idx_system_tokens_token ON system_tokens(token) 
   WHERE is_active = true;
   ```
   → Busca rápida de tokens ativos

2. **Limpeza Automática:**
   ```sql
   CREATE FUNCTION cleanup_old_system_token_logs()
   ```
   → Remove logs > 90 dias

3. **Validação em Memória:**
   - IP check usa algoritmos eficientes
   - Sem chamadas extras ao banco após validação inicial

### Carga Esperada:

- ✅ 1000+ req/s por token (com índices)
- ✅ Logs assíncronos (não bloqueiam requisição)
- ✅ Cache de tokens possível (futuro)

## Extensões Futuras

### Possíveis Melhorias:

1. **Rate Limiting por Token:**
   ```typescript
   max_requests_per_minute: 100
   ```

2. **Scopes/Permissões:**
   ```typescript
   allowed_endpoints: ["/api/agents/*", "/api/chat/*"]
   ```

3. **Notificações:**
   - Email quando token é usado de IP novo
   - Alerta de uso suspeito

4. **Rotação Automática:**
   ```typescript
   rotate_every_days: 90
   ```

5. **Cache Redis:**
   - Cache de tokens válidos
   - Invalidação automática ao revogar

---

**Versão:** 1.0  
**Data:** 2024-01-28  
**Status:** ✅ Implementado e Testado
